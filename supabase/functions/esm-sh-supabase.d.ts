/** Type declarations for Deno URL imports (esm.sh) used by Edge Functions. */
declare module 'https://esm.sh/@supabase/supabase-js@2' {
  export function createClient(supabaseUrl: string, supabaseKey: string, options?: object): any;
}
